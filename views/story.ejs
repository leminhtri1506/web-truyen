<%- include('partials/header') %>

<div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <div class="md:flex">
        <div class="md:w-1/3 lg:w-1/4 p-4">
            <img src="<%= story.cover || 'https://via.placeholder.com/300x400' %>" 
                 class="w-full h-auto object-cover rounded shadow-md border border-gray-200">
        </div>

        <div class="md:w-2/3 lg:w-3/4 p-6">
            <h1 class="text-3xl font-bold text-indigo-900 mb-2"><%= story.title %></h1>
            
            <div class="flex flex-wrap gap-2 mb-4 items-center">
                <% if(tags && tags.length > 0) { %>
                    <% tags.forEach(t => { %> 
                        <span class="bg-gray-200 text-gray-700 px-2 py-1 text-xs rounded-full border border-gray-300">
                            <%= t.name %>
                        </span> 
                    <% }) %>
                <% } %>

                <% if(story.status === 'Đã hoàn thành') { %>
                    <span class="bg-green-600 text-white text-xs font-bold px-3 py-1 rounded shadow">Full</span>
                <% } else { %>
                    <span class="bg-blue-600 text-white text-xs font-bold px-3 py-1 rounded shadow">Đang cập nhật</span>
                <% } %>
            </div>

            <div class="text-gray-600 leading-relaxed mb-6 whitespace-pre-line text-justify border-t pt-4">
                <%= story.description %>
            </div>
        </div>
    </div>

    <% if(user && user.role === 'admin') { %>
        <div class="bg-yellow-50 border-t-4 border-yellow-400 p-6 mx-4 mb-6 rounded shadow-inner">
            <h3 class="font-bold text-yellow-800 text-lg mb-3 flex items-center">
                🛠 Admin Panel: Thêm chương mới
            </h3>
            <form action="/admin/add-chapter" method="POST">
                <input type="hidden" name="story_id" value="<%= story.id %>">
                
                <div class="mb-3">
                    <label class="block text-sm font-bold text-gray-700 mb-1">Tên chương:</label>
                    <input type="text" name="title" placeholder="Ví dụ: Chương 10: Đại chiến" 
                           class="w-full border p-2 rounded focus:outline-none focus:border-indigo-500" required>
                </div>
                
                <div class="mb-3">
                    <label class="block text-sm font-bold text-gray-700 mb-1">Nội dung chương:</label>
                    <textarea name="content" rows="10" placeholder="Nội dung truyện..." 
                              class="w-full border p-2 rounded focus:outline-none focus:border-indigo-500 font-serif" required></textarea>
                </div>

                <button class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-6 rounded shadow transition">
                    Đăng Chương Này
                </button>
            </form>
        </div>
    <% } %>

    <div class="bg-gray-50 p-6 border-t">
        <h2 class="text-xl font-bold text-gray-800 mb-4 border-l-4 border-indigo-600 pl-3">Danh sách chương</h2>
        
        <% if(chapters.length === 0) { %>
            <p class="text-gray-500 italic ml-4">Chưa có chương nào được đăng.</p>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-2">
                <% chapters.forEach(c => { %>
                    <a href="/read/<%= c.id %>" class="block bg-white p-3 rounded border hover:bg-indigo-50 hover:border-indigo-300 transition truncate text-gray-700">
                        📄 <%= c.title %>
                    </a>
                <% }) %>
            </div>
        <% } %>
    </div>
</div>

<%- include('partials/footer') %>
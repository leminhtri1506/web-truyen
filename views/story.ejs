<%- include('partials/header') %>

<div class="bg-white rounded-lg shadow-lg overflow-hidden min-h-screen">
    <div class="md:flex">
        <div class="md:w-1/3 lg:w-1/4 p-4">
            <div class="relative group">
                <img src="<%= story.cover || 'https://via.placeholder.com/300x400' %>" 
                     class="w-full h-auto object-cover rounded shadow-md border border-gray-200">
                <div class="absolute inset-0 bg-black opacity-0 group-hover:opacity-10 transition rounded"></div>
            </div>
        </div>

        <div class="md:w-2/3 lg:w-3/4 p-6">
            <h1 class="text-3xl font-bold text-indigo-900 mb-2 font-ui"><%= story.title %></h1>
            
            <div class="flex flex-wrap gap-2 mb-6 items-center">
                <% if(story.status === 'Đã hoàn thành') { %>
                    <span class="bg-green-600 text-white text-xs font-bold px-3 py-1 rounded shadow">Full</span>
                <% } else { %>
                    <span class="bg-blue-600 text-white text-xs font-bold px-3 py-1 rounded shadow">Đang cập nhật</span>
                <% } %>

                <% if(tags && tags.length > 0) { %>
                    <% tags.forEach(t => { %> 
                        <span class="bg-gray-100 text-gray-600 px-2 py-1 text-xs rounded border border-gray-300">
                            #<%= t.name %>
                        </span> 
                    <% }) %>
                <% } %>
            </div>

            <div class="mb-6 flex gap-3">
                <% if(chapters && chapters.length > 0) { %>
                    <a href="/read/<%= chapters[0].id %>" class="bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-8 rounded-full shadow-lg transition transform hover:-translate-y-1 flex items-center gap-2">
                        <span>📖</span> Đọc Từ Đầu
                    </a>
                    
                    <a href="/read/<%= chapters[chapters.length - 1].id %>" class="bg-white border border-gray-300 text-gray-700 font-bold py-3 px-6 rounded-full shadow hover:bg-gray-50 transition">
                        Chương Mới Nhất
                    </a>
                <% } else { %>
                    <button class="bg-gray-300 text-white font-bold py-3 px-8 rounded-full cursor-not-allowed">
                        Chưa có chương
                    </button>
                <% } %>
            </div>

            <div class="text-gray-600 leading-relaxed mb-6 whitespace-pre-line text-justify border-t pt-4 font-reading">
                <%= story.description %>
            </div>
        </div>
    </div>

    <% if(user && user.role === 'admin') { %>
        <div class="px-6 mb-4">
            <details class="group bg-yellow-50 border border-yellow-200 rounded-lg overflow-hidden">
                <summary class="flex justify-between items-center font-medium cursor-pointer list-none p-4 bg-yellow-100 text-yellow-800 hover:bg-yellow-200 transition">
                    <span class="flex items-center gap-2">
                        ⚙️ Admin Panel: Quản lý chương
                    </span>
                    <span class="transition group-open:rotate-180">
                        <svg fill="none" height="24" shape-rendering="geometricPrecision" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" viewBox="0 0 24 24" width="24"><path d="M6 9l6 6 6-6"></path></svg>
                    </span>
                </summary>
                
                <div class="text-neutral-600 p-6 border-t border-yellow-200">
                    <form action="/admin/add-chapter" method="POST">
                        <input type="hidden" name="story_id" value="<%= story.id %>">
                        
                        <div class="mb-3">
                            <label class="block text-sm font-bold text-gray-700 mb-1">Tên chương:</label>
                            <input type="text" name="title" placeholder="Ví dụ: Chương 10: Đại chiến" 
                                   class="w-full border p-2 rounded focus:outline-none focus:border-indigo-500" required>
                        </div>
                        
                        <div class="mb-3">
                            <label class="block text-sm font-bold text-gray-700 mb-1">Nội dung chương:</label>
                            <textarea name="content" rows="10" placeholder="Paste nội dung vào đây..." 
                                      class="w-full border p-2 rounded focus:outline-none focus:border-indigo-500 font-mono text-sm" required></textarea>
                        </div>
        
                        <button class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-6 rounded shadow transition">
                            Đăng Chương Này
                        </button>
                    </form>
                </div>
            </details>
        </div>
    <% } %>

    <div class="bg-gray-50 p-6 border-t">
        <h2 class="text-xl font-bold text-gray-800 mb-4 border-l-4 border-indigo-600 pl-3 flex items-center gap-2">
            <span>📑</span> Danh sách chương (<%= chapters.length %>)
        </h2>
        
        <% if(chapters.length === 0) { %>
            <p class="text-gray-500 italic ml-4">Chưa có chương nào được đăng.</p>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                <% chapters.forEach(c => { %>
                    <div class="flex items-center justify-between bg-white p-3 rounded border hover:shadow-sm transition group">
                        <a href="/read/<%= c.id %>" class="truncate text-gray-700 text-sm font-medium hover:text-indigo-600 flex-1">
                            <%= c.title %>
                        </a>

                        <% if(user && user.role === 'admin') { %>
                            <div class="flex gap-2 ml-2 opacity-100 md:opacity-0 group-hover:opacity-100 transition-opacity">
                                <a href="/admin/edit-chapter/<%= c.id %>" class="text-xs bg-yellow-100 text-yellow-700 px-2 py-1 rounded border border-yellow-300 hover:bg-yellow-200">
                                    Sửa
                                </a>
                                <a href="/admin/delete-chapter/<%= c.id %>" onclick="return confirm('Bạn có chắc chắn muốn xóa chương này? Hành động này không thể hoàn tác!')" class="text-xs bg-red-100 text-red-700 px-2 py-1 rounded border border-red-300 hover:bg-red-200">
                                    Xóa
                                </a>
                            </div>
                        <% } %>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</div>

<%- include('partials/footer') %>
<%- include('partials/header') %>

<div class="max-w-4xl mx-auto mt-10 p-6 bg-white rounded-lg shadow-lg">
    <div class="flex items-center justify-between border-b pb-4 mb-6">
        <h1 class="text-2xl font-bold text-gray-800">✏️ Chỉnh Sửa Chương</h1>
        <a href="/story/<%= chapter.story_id %>" class="text-gray-500 hover:text-gray-700">Quay lại truyện</a>
    </div>

    <form action="/admin/edit-chapter/<%= chapter.id %>" method="POST">
        <div class="mb-4">
            <label class="block text-gray-700 font-bold mb-2">Tên chương:</label>
            <input type="text" name="title" value="<%= chapter.title %>"
                   class="w-full border p-3 rounded focus:outline-none focus:border-indigo-500 shadow-sm" required>
        </div>

        <div class="mb-6">
            <label class="block text-gray-700 font-bold mb-2">Nội dung:</label>
            <textarea name="content" rows="20" 
                      class="w-full border p-3 rounded focus:outline-none focus:border-indigo-500 font-mono text-sm bg-gray-50" required><%= chapter.content %></textarea>
        </div>

        <div class="flex items-center gap-4">
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-8 rounded shadow transition flex items-center gap-2">
                <span>💾</span> Lưu Thay Đổi
            </button>

            <a href="/story/<%= chapter.story_id %>" class="text-gray-600 hover:underline">Hủy bỏ</a>
        </div>
    </form>
</div>

<%- include('partials/footer') %>